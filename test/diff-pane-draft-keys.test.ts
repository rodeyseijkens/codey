import { describe, expect, test } from "bun:test";

describe("diff pane cursor during a comment draft", () => {
  test("cursor remains unchanged while composing (layer-based gating)", () => {
    expect(true).toBe(true);
  });
});
